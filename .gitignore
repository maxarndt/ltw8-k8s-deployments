.DS_Store

# Never commit rendered secrets or local credential dumps.
*-secret.local.yaml
*-credentials.local.yaml
kubeconfig

# Talos: CA keys, encryption secret, admin cert (talos/ has its own .gitignore too)
talos/secrets.yaml
talos/generated/

