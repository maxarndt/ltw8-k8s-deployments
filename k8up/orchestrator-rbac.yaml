---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: backup-orchestrator
  namespace: k8up-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: backup-orchestrator
rules:
  - apiGroups: ["apps"]
    resources: ["deployments", "deployments/scale"]
    verbs: ["get", "list", "patch", "update"]
  - apiGroups: [""]
    resources: ["pods"]
    # delete is needed because Succeeded scale-down pods linger until PodGC,
    # and `kubectl wait --for=delete` would otherwise time out.
    verbs: ["get", "list", "watch", "delete"]
  - apiGroups: ["k8up.io"]
    resources: ["backups"]
    verbs: ["get", "list", "watch", "create", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: backup-orchestrator
subjects:
  - kind: ServiceAccount
    name: backup-orchestrator
    namespace: k8up-system
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: backup-orchestrator
